# {{ salt['pillar.get']('hardening:message_do_not_modify','') }}

# Prevent core dumps for all users. These are usually only needed by developers
# and may contain sensitive information.
* hard core 0
